import React from 'react'

function ProfilePictureHolder({size}) {
  return (
    <div className={`rounded-full bg-slate-700 ${size}`}>
        <img/>
    </div>
  )
}

export default ProfilePictureHolder